[build]
jobs = 10
# sccache disabled for local dev — incremental compilation is faster when the
# sccache cache is cold. Re-enable for CI with: rustc-wrapper = "sccache" + incremental = false
# rustc-wrapper = "sccache"
# incremental = false
rustc-wrapper = "/Users/cristian/nexusdb/tools/rustc-macos.sh"

[target.aarch64-apple-darwin]
linker = "/Users/cristian/nexusdb/tools/linker-macos.sh"
