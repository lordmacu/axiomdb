name: axiomdb
description: AxiomDB embedded database — Dart/Flutter binding via dart:ffi.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  ffi: ^2.1.0

dev_dependencies:
  test: ^1.25.0
  sqlite3: ^2.4.0
